const aData = [1, 2, 3, 4, 5];
console.log(aData);
console.log(aData.length);
aData[35] = 17;
console.log(aData);
console.log('length: ' + aData.length);
console.log(aData.length);
console.log(aData[30]);
console.log(aData[100]);

// Test, usar el 'expected().toEqual(), porque 2 arrays no pueden ser toBe()

const aLength = (aData) => {
  do {} while (condition);

  return aData.aLenght;
};

const aPush = (aData, algo) => {
  // Usar la función aLength
};
